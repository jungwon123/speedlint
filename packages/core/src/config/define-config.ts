import type { SpeedlintConfig } from "../types/index.js";

/**
 * Helper to define a type-safe speedlint configuration.
 */
export function defineConfig(config: SpeedlintConfig): SpeedlintConfig {
	return config;
}
